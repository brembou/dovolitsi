{{!< default}}

{{!--

There are two error files in this theme, one for 404s and one for all other errors.
This file is the former, and handles all 404 Page Not Found errors.

The 404 error is the most common error that a visitor might see, for example when
following a broken link


--}}

<div class="container">

    <section class="h-[55vh] mt-10 text-center error-message">
        <h1 class="font-bold text-9xl error-code font-display">{{statusCode}}</h1>
        <p class="text-base error-description">{{message}}</p>
        <a class="mt-8 error-link inline-flex items-center justify-center px-8 text-sm h-[42px] leading-none tracking-wider text-white uppercase transition-opacity macaw-button font-display bg-acc rounded-3xl hover:opacity-80" href="{{@site.url}}">{{t "Go to the front page"}} →</a>
    </section>

</div>

<script>
    document.addEventListener('DOMContentLoaded', function() { plausible('404'); });
</script>